# Copy to .env.development.local if you need to override during dev.
# Leave unset for npm run dev → uses /data/... from public/data/

# Production / preview: set in .env.production (committed) or CI
# VITE_DATA_BASE_URL=https://example.r2.dev
